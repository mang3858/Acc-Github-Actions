name: Slack Notify

on:
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Slack message
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{"text":"GitHub Actions 실습 완료"}' \
          ${{ secrets.WEBHOOK_URL }}
